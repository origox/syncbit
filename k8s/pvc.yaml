apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: syncbit-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Optional: specify storage class
  # storageClassName: your-storage-class
